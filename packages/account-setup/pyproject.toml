[project]
name = "account-setup"
version = "0.1.0"
description = "Vendor account creation tool for CrewCircle infrastructure"
requires-python = ">=3.11"
dependencies = [
    "playwright",
]

[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.backends._legacy:_Backend"
